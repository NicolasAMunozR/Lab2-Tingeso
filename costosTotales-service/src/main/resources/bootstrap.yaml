spring:
  application:
    name: costosTotales-service
  cloud:
    config:
      enabled: true
      uri: http://localhost:8081
  datasource:
    url: jdbc:postgresql://localhost:5432/costosTotales
    username: postgresql
    password: alonsoreyes104
  jpa:
    hibernate:
      ddl-auto: update