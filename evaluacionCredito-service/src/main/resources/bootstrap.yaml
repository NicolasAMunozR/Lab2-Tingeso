spring:
  application:
    name: evaluacionCredito-service
  cloud:
    config:
      enabled: true
      uri: http://localhost:8081
  datasource:
    url: jdbc:postgresql://localhost:5432/evaluacionCredito
    username: postgres
    password: alonsoreyes104
  jpa:
    hibernate:
      ddl-auto: update