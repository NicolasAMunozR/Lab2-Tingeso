spring:
  servlet:
    multipart:
      max-file-size: 10MB
      max-request-size: 10MB
  application:
    name: solicitudCredito-service
  cloud:
    config:
      enabled: true
      uri: http://localhost:8081
  datasource:
    url: jdbc:postgresql://localhost:5432/solicitudCredito
    username: postgres
    password: alonsoreyes104
  jpa:
    hibernate:
      ddl-auto: update