spring:
  application:
    name: simulacionCredito-service
  cloud:
    config:
      enabled: true
      uri: http://localhost:8081
  datasource:
    url: jdbc:postgresql://localhost:5432/simulacionCredito
    username: postgresql
    password: alonsoreyes104
  jpa:
    hibernate:
      ddl-auto: update